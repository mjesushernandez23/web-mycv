import axios from "axios";
import { API } from "@utils/constants";

export default async function uploadApi(files: FileList) {
  const url = `${API}/upload/`;

  try {
    const response = axios.postForm;
  } catch (error) {}
}
